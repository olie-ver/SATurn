# SATurn
This SAT solver (built with recursive backtracking, unit clause propagation, pure literal elimination,
and literal contradiction detection - ie, a complete DPLL SAT solver)
is completely self contained and can be compiled and run (if put in a main function) using C++20.