#include "SAT.hpp"
#include <algorithm>

namespace SATurn {
    // void SATSolver::create_watched() {
    //     for (size_t i = 0; i < clauses.size(); i++) {
    //         const std::vector<int>& clause = clauses[i];
    //         for (size_t j = 0; j < clause.size(); j++) {
    //             watched_literals[std::abs(clause[j])].push_back(clause);
    //         }
    //     }
    // }
}