# SATurn
This SAT solver (built with recursive backtracking, unit clause propagation, and pure literal elimination)
is completely self contained and can be compiled and run (if put in a main function) using C++20.